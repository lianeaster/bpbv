{
  "name": "bpbv",
  "version": "1.3.0",
  "private": true,
  "scripts": {
    "start": "npx -y serve -l 3000 -s --listen tcp://0.0.0.0:3000",
    "stop": "echo 'Зупиніть сервер: Ctrl+C у терміналі де він запущений'",
    "fetch-news": "node fetch-news.js",
    "fetch-news:dry": "node fetch-news.js --dry-run"
  },
  "devDependencies": {
    "puppeteer": "^24.40.0"
  }
}
